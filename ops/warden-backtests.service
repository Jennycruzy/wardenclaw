[Unit]
Description=WARDENCLAW refresh real-candle xStock backtests
After=network-online.target
Wants=network-online.target

[Service]
Type=oneshot
User=root
WorkingDirectory=/root/wardenbitget
Environment=PATH=/usr/local/bin:/usr/bin:/bin
ExecStart=/usr/bin/bash -lc 'cd /root/wardenbitget && pnpm backtest:all'

